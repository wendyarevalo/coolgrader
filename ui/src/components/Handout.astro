---
export interface Props {
	title: string;
	body: string;
}

const { title, body } = Astro.props;
---

<section>
    <h2>{title}</h2>
    <article>
        <Fragment set:html={body} />
    </article>
</section>